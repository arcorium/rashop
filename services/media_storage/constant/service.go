package constant

const (
  ServiceName        = "media_storage"
  ServiceVersion     = "v1"
  ServiceCommandName = ServiceName + "::command"
  ServiceQueryName   = ServiceName + "::query"
)

const (
  StorageBucket = "media"
)
